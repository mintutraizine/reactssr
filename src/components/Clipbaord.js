import CopyToClipboard from "react-copy-to-clipboard";
    const Clipbaord = (texttocopy) => {
        onCopy(texttocopy, result)
        alert(`You have copied "${texttocopy}"`);
     }

     export default Clipbaord;